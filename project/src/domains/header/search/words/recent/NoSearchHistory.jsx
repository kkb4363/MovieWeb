import styled from 'styled-components';

const Wrapper = styled.div`
  font-size: 20px;
`;

const NoSearchHistory = () => {
  return <Wrapper>검색 내역이 없습니다.</Wrapper>;
};

export default NoSearchHistory;
